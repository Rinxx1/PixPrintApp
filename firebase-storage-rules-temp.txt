// TEMPORARY Firebase Storage Rules - FOR TESTING ONLY
// ⚠️ WARNING: These rules are very permissive. Use only for testing!
// Replace with proper rules before going to production.

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to all authenticated users
      allow read, write: if request.auth != null;
      
      // Allow read access to everyone (for viewing photos)
      allow read: if true;
    }
  }
}
